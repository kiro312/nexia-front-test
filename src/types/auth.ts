export type RegisterData = {
    username: string;
    password: string;
    birthDate: string;
    nationality: string;
    gender: boolean;
    role: string;
    parentPin: string;
}

export type LoginData = {
    username: string;
    password: string;
}